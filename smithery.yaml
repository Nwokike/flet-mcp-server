version: "1"
startCommand:
  type: stdio
configSchema:
  type: object
  properties: {}
deploy:
  docker:
    dockerfile: Dockerfile
commandFunction:
  |-
  (config) => ({
    command: 'uvx',
    args: ['--quiet', 'flet-mcp-server']
  })
