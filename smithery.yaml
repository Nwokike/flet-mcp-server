version: "1"
startCommand:
  type: stdio
configSchema:
  type: object
  properties: {}
deploy:
  docker:
    dockerfile: Dockerfile
commandFunction:
  |-
  (config) => ({
    command: 'uv',
    args: ['run', '--quiet', 'flet-mcp-server']
  })
